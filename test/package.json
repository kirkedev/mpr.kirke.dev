{
    "name": "test",
    "version": "0.1.0",
    "scripts": {
        "api": "cypress run --project api",
        "integration": "yarn api",
        "unit": "jest --projects lib",
        "all": "yarn unit && yarn integration",
        "cypress": "cypress open --detached --global --project api",
        "server": "node mpr"
    },
    "devDependencies": {
        "@types/jest": "^27.0.2",
        "@types/node": "^16.10.9",
        "jest": "^27.2.5",
        "ts-jest": "^27.0.5",
        "typescript": "^4.4.4"
    },
    "dependencies": {
        "cypress": "^8.6.0",
        "lib": "workspace:*"
    }
}
