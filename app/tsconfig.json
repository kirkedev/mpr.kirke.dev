{
    "include": ["**/*.ts", "**/*.svelte", "**/*.css"],
    "references": [
        { "path":  "../lib" }
    ],
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "composite": true
    }
}
