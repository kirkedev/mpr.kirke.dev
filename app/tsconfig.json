{
    "include": ["**/*.ts", "**/*.svelte", "**/*.css", "*.js"],
    "references": [
        { "path":  "../lib" }
    ],
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "composite": true
    }
}
