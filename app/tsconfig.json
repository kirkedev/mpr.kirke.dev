{
    "rootDir": "..",
    "include": ["**/*.ts", "**/*.d.ts", "**/*.svelte", "**/*.css"],
    "references": [
        { "path":  "../lib" }
    ],
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "composite": true,
        "types": ["vite/client"]
    }
}
