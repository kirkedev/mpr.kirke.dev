{
    "name": "features",
    "private": true,
    "type": "commonjs",
    "scripts": {
        "open": "cypress open --detached",
        "test": "cypress run"
    },
    "devDependencies": {
        "@types/resolve": "^1.20.2",
        "esbuild": "^0.19.3"
    },
    "dependencies": {
        "@badeball/cypress-cucumber-preprocessor": "^18.0.6",
        "cypress": "^13.3.0",
        "resolve": "^1.22.6",
        "typescript": "^5.2.2"
    },
    "cypress-cucumber-preprocessor": {
        "e2e": {
            "stepDefinitions": "**/*.steps.ts"
        }
    }
}
